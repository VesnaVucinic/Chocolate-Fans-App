<h1> All the Reviews</h1>
<% @reviews.each do |review| %>

  <li>
  <%= review.chocolate.title%> in <%= review.chocolate.category%> chocolates category: <%= link_to review.title, review_path(review)%> - <%= review.stars%>
  </li> <br>

<% end %>